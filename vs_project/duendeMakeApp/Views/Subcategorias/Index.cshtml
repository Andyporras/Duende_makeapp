@model IEnumerable<duendeMakeApp.Models.Subcategoria>

@{
    ViewData["Title"] = "Index";
}

@try
{
    @if (ViewBag.Usuario.TipoId == 1)
    {
        <h1>Sub categoria</h1>

        <p>
            <a class="btn btn-primary" asp-action="Create">Crear Subcategoria</a>
        </p>
        <input class="form-control" id="myInput" type="text" placeholder="Buscar..">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Nombre)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nombre)
                        </td>
                        <td>
                            <a class="btn btn-secondary" asp-action="Edit" asp-route-id="@item.SubcategoriaId">Edit</a> |
                            @* <a asp-action="Details" asp-route-id="@item.SubcategoriaId">Details</a> | *@
                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.SubcategoriaId">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-danger">Acceso ddenegado.</div>
    }

}
catch (Exception ex)
{
    <div class="alert alert-danger">Acceso ddenegado.</div>
}